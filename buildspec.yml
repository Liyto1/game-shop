version: 0.2
phases:
  build:
    commands:
      - echo Packaging JAR file..
      - mvn clean package
  post_build:
    commands:
      - echo Build complete
      - mv target/*.jar app.jar
artifacts:
  files:
    - app.jar
  discard-path: yes